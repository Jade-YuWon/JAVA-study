```
Object I/O by serialization / deserialization
: writeObject / readObject.
public class SomeClass0 """implements Serializable"""

outputStream.writeObject(oneRecord);
readOne = (Species)inputStream.readObject();
```
